package com.example.android.java;

import java.util.ArrayList;

public class Words_Dictionary {
    public static  void main(String[] args){
        ArrayList<String> match = new ArrayList<String>();
        //for each word in dict
       /* for(String word: list){
            //match flag
            Boolean nonmatch =true;

            //for each character of dict word
            for(char chW : word.toCharArray()){
                String w = Character.toString(chW);
                //if the count of chW in word is equal to its count in input, then they are match
                if(word.length()-word.replace(w,"").length()!=input.length()-input.)
            }
        }*/
    }
}
